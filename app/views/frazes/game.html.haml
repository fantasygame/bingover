%table.table.table.striped
  - @user_frazes.each_slice(5) do |slice|
    %tr
      - slice.each do |user_fraze|
        %td
          - if user_fraze.checked?
            %strong X
          = link_to user_fraze.fraze.body, toggle_check_frazes_path(user_fraze_id: user_fraze.id)


